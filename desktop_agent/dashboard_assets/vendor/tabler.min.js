/*!
 * Tabler-inspired local vendor helper
 * Source reference: https://github.com/tabler/tabler
 * License: MIT
 */
(function (global) {
  "use strict";

  function dispatch(name, detail) {
    try {
      global.dispatchEvent(new CustomEvent(name, { detail }));
    } catch (_err) {
      // no-op for compatibility environments
    }
  }

  global.Tabler = global.Tabler || {};
  global.Tabler.version = "local-curated-1";
  global.Tabler.emit = dispatch;
})(globalThis);
